jumpForce = 16;

image_speed = 0;

acos = cos(degtorad(image_angle));
asin = sin(degtorad(image_angle));

grv = 1;

sound_delay = 60;
sound_maxDelay = 60;

selected = false; //check if the spring is the player's target
//lock on animation

frames = 0;
maxFrames = 10;
index = 0;